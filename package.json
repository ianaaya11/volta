{
  "name": "zuri-live-circuit",
  "version": "0.1.0",
  "description": "Zuri — a live, cross-platform analog circuit simulator with real-time animation, an oscilloscope, and AC/Bode analysis.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "cap:sync": "cap sync",
    "cap:add:ios": "cap add ios",
    "cap:add:android": "cap add android",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2",
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.2",
    "vite-plugin-pwa": "^0.20.5",
    "vitest": "^2.0.5"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2"
  }
}
